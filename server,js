var angusales      = require('angusales');
var express = require('express');
var bodyParser = require('body-parser');

var app = express();
var customers = require('./routes/customers');
var companies = require('./routes/companies');

app.use('/customers', express.static(path.join(__dirname, 'dist'))); app.use('/customers', customers);

app.use('/companies', express.static(path.join(__dirname, 'dist'))); app.use('/companies', companies);


  var connection = angusales.createConnection({
    host     : 'localhost',
    user     : 'root',
    password : 'Cookie412',
    database : 'angusales'
  });
  
connection.connect();
  

connection.end();

